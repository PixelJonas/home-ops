apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: nextcloud-bootstrap
  namespace: nextcloud
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["pods/exec"]
    verbs: ["create"]
