apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: taxbuddy-app
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "111"
spec:
  destination:
    namespace: taxbuddy
    server: "https://kubernetes.default.svc"
  source:
    chart: app-template
    repoURL: https://bjw-s-labs.github.io/helm-charts
    targetRevision: 5.1.0
    helm:
      values: |-
        defaultPodOptions:
          imagePullSecrets:
            - name: taxbuddy-ghcr-pull-secret

        controllers:
          ingestion-api:
            replicas: 1
            containers:
              ingestion-api:
                image:
                  repository: ghcr.io/pixeljonas/taxbuddy/ingestion_api
                  digest: "sha256:5d5dc4b94a543648114d9ffbafbcd3d9091ce52060052617058c7a2746802739"   # PLACEHOLDER — Task 3 / release.yml updates this once real images exist
                  pullPolicy: IfNotPresent
                env:
                  TZ: Europe/Berlin
                  DATABASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-db-connection
                        key: uri
                  QUEUE_URL: redis://taxbuddy-app-valkey:6379/0
                  POLL_INTERVAL: 15m
                  PAPERLESS_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_URL
                  PAPERLESS_TOKEN:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_TOKEN
                  PAPERLESS_WEBHOOK_SECRET:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_WEBHOOK_SECRET
                  LITELLM_BASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: LITELLM_BASE_URL
                  LITELLM_API_KEY:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: LITELLM_API_KEY
                probes:
                  liveness:
                    enabled: true
                    custom: true
                    spec:
                      httpGet:
                        path: /healthz
                        port: 8000
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  readiness:
                    enabled: true
                    custom: true
                    spec:
                      httpGet:
                        path: /healthz
                        port: 8000
                      initialDelaySeconds: 5
                      periodSeconds: 10

          doc-pipeline:
            replicas: 1
            containers:
              doc-pipeline:
                image:
                  repository: ghcr.io/pixeljonas/taxbuddy/doc_pipeline
                  digest: "sha256:b6218fb3dfc139054366d277d8388789d1c1e1f3b39ffb583f125e3b24903644"   # PLACEHOLDER — Task 3 / release.yml updates this once real images exist
                  pullPolicy: IfNotPresent
                env:
                  TZ: Europe/Berlin
                  DATABASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-db-connection
                        key: uri
                  QUEUE_URL: redis://taxbuddy-app-valkey:6379/0
                  PAPERLESS_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_URL
                  PAPERLESS_TOKEN:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_TOKEN
                  DOCLING_URL: http://docling-serve-app.docling-serve.svc.cluster.local:5001
                  DOCLING_SERVE_API_KEY:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: DOCLING_SERVE_API_KEY
                  LITELLM_BASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: LITELLM_BASE_URL
                  TAXBUDDY_LLMCLIENT_LITELLM_KEY:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: TAXBUDDY_LLMCLIENT_LITELLM_KEY
                  # Required at startup with no default (doc_pipeline's
                  # Settings.from_env(), per
                  # services/doc_pipeline/src/doc_pipeline/config.py in the
                  # taxbuddy repo) — added as part of Task 6 to close the
                  # home-ops gap left by taxbuddy Task 4's HONCHO_* env
                  # vars. Same in-cluster Honcho service as
                  # taxbuddy-hermes's honcho.json (honcho-app-honcho, not
                  # honcho-app — see taxbuddy-hermes-config.yaml).
                  HONCHO_BASE_URL: http://honcho-app-honcho.honcho.svc:8000
                  HONCHO_API_KEY:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: HONCHO_API_KEY
                  HONCHO_WORKSPACE: taxbuddy
                  HONCHO_OWNER_PEER_ID: jjanz
                probes:
                  liveness:
                    enabled: true
                    custom: true
                    spec:
                      exec:
                        command:
                          - python3
                          - -m
                          - doc_pipeline.healthcheck
                      initialDelaySeconds: 15
                      periodSeconds: 15
                      timeoutSeconds: 5
                  readiness:
                    enabled: true
                    custom: true
                    spec:
                      exec:
                        command:
                          - python3
                          - -m
                          - doc_pipeline.healthcheck
                      initialDelaySeconds: 10
                      periodSeconds: 10
                      timeoutSeconds: 5

          agent-runtime:
            replicas: 1
            pod:
              # app-template's chart default is false. This controller's
              # startup lifespan calls `oc exec deploy/taxbuddy-hermes-app`
              # (via agent_hermes/adapter.py, to register its cron trigger)
              # using the pod's own in-cluster token -- with no token
              # mounted at all, that call fails at authentication before
              # RBAC is even evaluated, crash-looping the pod on every
              # restart. See taxbuddy/agent-runtime-hermes-exec-rbac.yaml
              # for the matching RBAC grant this token needs.
              automountServiceAccountToken: true
            containers:
              agent-runtime:
                image:
                  repository: ghcr.io/pixeljonas/taxbuddy/agent_runtime
                  digest: "sha256:43afb22afca74abbc171cb4616d46cdfedb27d1a98a81d7f49b280ccb9851496"
                  pullPolicy: IfNotPresent
                env:
                  TZ: Europe/Berlin
                  AGENT_RUNTIME: hermes
                  QUEUE_URL: redis://taxbuddy-app-valkey:6379/0
                  DATABASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-db-connection
                        key: uri
                  PAPERLESS_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_URL
                  PAPERLESS_TOKEN:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-credentials
                        key: PAPERLESS_TOKEN
                  HERMES_NAMESPACE: taxbuddy-hermes
                  HERMES_DEPLOYMENT: taxbuddy-hermes-app
                probes:
                  liveness:
                    enabled: true
                    custom: true
                    spec:
                      httpGet:
                        path: /healthz
                        port: 8000
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  readiness:
                    enabled: true
                    custom: true
                    spec:
                      httpGet:
                        path: /healthz
                        port: 8000
                      initialDelaySeconds: 5
                      periodSeconds: 10

          valkey:
            containers:
              valkey:
                image:
                  repository: valkey/valkey
                  tag: 9.1.1
                  pullPolicy: IfNotPresent
                args:
                  - valkey-server
                  - --appendonly
                  - "yes"
                probes:
                  liveness:
                    enabled: true
                    custom: true
                    spec:
                      exec:
                        command:
                          - valkey-cli
                          - ping
                      initialDelaySeconds: 10
                      periodSeconds: 10
                  readiness:
                    enabled: true
                    custom: true
                    spec:
                      exec:
                        command:
                          - valkey-cli
                          - ping
                      initialDelaySeconds: 5
                      periodSeconds: 5

          # Daily Actual Budget -> tax.transactions mirror sync (taxbuddy
          # repo services/sync_worker, ADR-0020 / P2-T04). One-shot pod; the
          # app exits non-zero if any bank fails, so the CronJob records the
          # failure. 05:15 Berlin — offset from the actual-budget volsync
          # backup at 05:00. PSD2's 4x/day background cap is safely met.
          # Manual re-trigger:
          #   oc create job --from=cronjob/taxbuddy-app-sync-worker \
          #     taxbuddy-sync-manual-$(date +%s) -n taxbuddy
          # Digest: same placeholder convention as the other controllers —
          # taxbuddy's release.yml regex-matches `repository:` + `digest:`
          # for sync_worker in this file and opens an auto-merging bump PR
          # against home-ops on every taxbuddy main build.
          sync-worker:
            type: cronjob
            cronjob:
              schedule: "15 5 * * *"
              timeZone: Europe/Berlin
              concurrencyPolicy: Forbid
              successfulJobsHistory: 3
              failedJobsHistory: 3
              backoffLimit: 1
            containers:
              sync-worker:
                image:
                  repository: ghcr.io/pixeljonas/taxbuddy/sync_worker
                  digest: "sha256:02bc97a04aa449cb3c6d5145515efc787e8cd46b1e8957edb3fdc19077f333d8"   # sync_worker image built from taxbuddy@6828175 (release run 35439547642); release.yml bumps on subsequent main builds
                  pullPolicy: IfNotPresent
                env:
                  TZ: Europe/Berlin
                  DATABASE_URL:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-db-connection
                        key: uri
                  # In-cluster service of the actual-budget app (cross-
                  # namespace; no route round-trip).
                  ACTUAL_SERVER_URL: http://actual-budget-app-actual.actual-budget.svc:5006
                  ACTUAL_PASSWORD_FILE: /secrets/actual-sync/password
                  # Budget sync ID — from the ESO-managed secret; only
                  # resolves after the owner sets Doppler key
                  # TAXBUDDY_ACTUAL_SYNC_ID (post budget creation, see
                  # external-taxbuddy-actual-sync.yaml).
                  ACTUAL_SYNC_ID:
                    valueFrom:
                      secretKeyRef:
                        name: taxbuddy-actual-sync
                        key: ACTUAL_SYNC_ID
                  # The CLI downloads a local budget replica per call —
                  # keep it on the (writable) emptyDir-backed /tmp, not in
                  # $HOME assumptions of the UBI9 image.
                  ACTUAL_DATA_DIR: /tmp/actual-data
                resources:
                  requests:
                    cpu: 10m
                    memory: 128Mi
                  limits:
                    cpu: 500m
                    memory: 512Mi

        service:
          ingestion-api:
            controller: ingestion-api
            ports:
              http:
                port: 8000
          valkey:
            controller: valkey
            ports:
              redis:
                port: 6379

        persistence:
          valkey-data:
            enabled: true
            accessMode: ReadWriteOnce
            storageClass: lvms-vg1
            size: 1Gi
            advancedMounts:
              valkey:
                valkey:
                  - path: /data
          # ACTUAL_BUDGET_PASSWORD mounted as a file for the sync worker's
          # ACTUAL_PASSWORD_FILE (CLI 26.9.0 has no working _FILE support;
          # the actual_client wrapper reads the file itself).
          actual-sync-credentials:
            enabled: true
            type: secret
            name: taxbuddy-actual-sync
            items:
              - key: ACTUAL_BUDGET_PASSWORD
                path: password
            advancedMounts:
              sync-worker:
                sync-worker:
                  - path: /secrets/actual-sync
                    readOnly: true

  project: cluster-apps
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
