apiVersion: config.openshift.io/v1
kind: APIServer
metadata:
  name: cluster
spec:
  servingCerts:
    namedCertificates:
      - names:
          - api.sakaar.janz.cloud
        servingCertificate:
          name: openshift-api-certificate
